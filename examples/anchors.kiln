// Anchors: which edges of the window a control keeps its distance from.
//
// The form is 400x300. Resize the window and the OK button stays in the
// bottom-right corner while the name box stretches with the width; the label
// has the default anchors (left,top) and stays where it was drawn. An anchor is
// set like any other property: `Anchors = "right,bottom";`.
//
//   kiln build examples/anchors.kiln -o anchors

namespace Anchors;

using Kiln.Ui;

public partial form Win
{
    Title = "Anchors";
    Width = 400;
    Height = 300;

    Button okButton
    {
        Text = "OK";
        Left = 250;
        Top = 230;
        Width = 120;
        Height = 36;
        Color = "#ffffff";
        Anchors = "right,bottom";
        Click += OnOk;
    }

    Editbox nameBox
    {
        Text = "stretches with the window";
        Left = 20;
        Top = 44;
        Width = 360;
        Height = 26;
        Anchors = "left,right";
    }

    Label caption
    {
        Text = "Resize the window.";
        Left = 20;
        Top = 20;
    }
}

public partial form Win
{
    // `okButton.Left` is read at the moment of the click, so after a resize it
    // reports where the anchor has moved the button to.
    void OnOk()
    {
        Console.WriteLine($"ok at {okButton.Left}");
    }
}
